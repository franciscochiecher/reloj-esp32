RELOJ ESP32 REMOTO - USUARIOS PERSISTENTES

El servidor guarda los usuarios en data/users.json.
En Render, para que sobrevivan reinicios/redeploys, configura un Persistent Disk y DATA_DIR=/var/data.
Variables: DEVICE_TOKEN=tu_token ; REGISTRATION_KEY=taller ; DATA_DIR=/var/data
